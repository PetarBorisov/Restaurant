spring:
  jpa:
    hibernate:
      ddl-datasource-initialization: true
      sql:
        init:
          mode: never
  mvc:
    hiddenmethod:
      filter:
        enabled: true

restaurant:
  default.admin.pass: Test
  remember.me.key: secret

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE



open.exchange.rates:
  schema: https
  host: dummy
  path: /api/latest.json
  app_id: test
  symbols: EUR,BGN
  enabled: false

mail:
  restaurant: "restaurant@example.com"
  host: localhost
  port: 3333
  username: test@example.com
  password: topsecret